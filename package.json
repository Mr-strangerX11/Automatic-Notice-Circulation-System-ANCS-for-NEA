{
  "homepage": "https://mr-strangerx11.github.io/Automatic-Notice-Circulation-System-ANCS-for-NEA",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "predeploy": "npm ci --prefix frontend && VITE_GH_PAGES_BASE=/Automatic-Notice-Circulation-System-ANCS-for-NEA/ npm run build --prefix frontend && cp frontend/dist/index.html frontend/dist/404.html",
    "deploy": "gh-pages -d frontend/dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
